# Todo: KI-Chat funktionsfähig machen

- [ ] 1. Agent-Init beim Start wieder aktivieren (hinter `--ai=off`); ohne Agent: Meldung im Chat statt endlos „thinking“, Lade-Zustand zurücksetzen
- [ ] 2. Statuspunkt neben „Gemma 4 Agent“ zeigt den echten Verbindungszustand (Agent bereit / initialisiert / fehlt), nicht nur „Modelldatei existiert“
- [ ] 3. E2E headless: Chat öffnen, Frage senden, Antwort vom laufenden Ollama erscheint; zweiter Lauf mit `--ai=off` prüft die Fehlermeldung
